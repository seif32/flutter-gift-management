import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFFFF715D);
  static const Color primaryVariant = Colors.red;

  // Secondary Colors
  static const Color secondary = Color(0xFF273238);
  static const Color background = Color(0xFFF9FAFB);

  // Additional Colors (optional)
  static const Color onPrimary = Colors.white; // Text/icon color on primary
  static const Color onSecondary = Colors.black; // Text/icon color on secondary
}
